import { Layout, DarkModeToggle } from '@oidanice/kindle-ui'

export default function Convert() {
  return (
    <Layout title="inkvert" headerRight={<DarkModeToggle />}>
      <p className="font-mono text-sm">Convert page placeholder</p>
    </Layout>
  )
}
